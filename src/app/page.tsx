// sections
import { HomeView } from 'src/sections/home/view';

// ----------------------------------------------------------------------

export const metadata = {
  title: 'Capture411: Home',
};

export default function HomePage() {
  return <HomeView />;
}
