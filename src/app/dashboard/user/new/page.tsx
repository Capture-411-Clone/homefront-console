import React from 'react';
import { UserNewView } from 'src/sections/user/view';

export const metadata = {
  title: 'New User',
};

export default function NewUserPage() {
  return <UserNewView />;
}
